version: '3.4'

services:
  myapp:
    image: mash404/portfolio
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
